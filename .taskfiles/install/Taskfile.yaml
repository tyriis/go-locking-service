---
# yaml-language-server: $schema=https://json.schemastore.org/taskfile.json
version: "3"

tasks:
  default:
    desc: Download the application dependencies
    cmds:
      - go mod download
      - go mod verify
